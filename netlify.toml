[build]
  # Sync from Notion before publishing
  command = "npm run sync-notion"
  publish = "."

[build.environment]
  NODE_VERSION = "18"

# Redirects for SPA-style navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
