[build]
  # Generate PDF documentation on each build
  command = "npm run generate-pdf"
  publish = "."

[build.environment]
  NODE_VERSION = "18"

# Redirects for SPA-style navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
